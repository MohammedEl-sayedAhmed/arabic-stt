# For the tests only (not needed to run Sedjem): the browser tests in tests/test_ui*.py drive Chromium
# with Playwright. They use the installed Chrome or Edge, or run `playwright install chromium` first.
playwright==1.63.0
